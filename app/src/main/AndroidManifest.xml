<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <!-- 后台清理组件：killBackgroundProcesses() 所需（normal 级，安装即授予） -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!-- 卸载应用：Intent.ACTION_DELETE 需要此权限（API 34+ 官方要求，但部分定制 ROM
         在 API 33 上也会检查；正常权限，安装即授予，不影响旧版 API）。 -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES"
        tools:targetApi="34" />

    <!-- jar 挂机保活前台服务（API 28+ 必需，缺失会导致 startForeground 抛
         SecurityException 并使 :midlet 进程崩溃、挂机失效） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- API 34+ 前台服务必须声明类型，本服务用 specialUse（对应权限如下） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:targetApi="34" />

    <!-- libsu（minSdk 19）与 mobileffmpeg（minSdk 高于 14）一样高于本工程 MIN_SDK=14，
         通过 overrideLibrary 绕过 manifest merger 的 minSdk 校验；运行时代码中已按
         Build.VERSION.SDK_INT < 19 守卫（仅 root 激活功能用到 libsu）。
         注意：overrideLibrary 值必须是库的 manifest package（libsu 为 com.topjohnwu.superuser），
         不是 maven 坐标 com.github.topjohnwu.libsu。 -->
    <uses-sdk tools:overrideLibrary="com.arthenica.mobileffmpeg,com.topjohnwu.superuser" />

    <!-- Android 11+ 包可见性：声明可查询带 LAUNCHER 入口的应用，
         以便功能表枚举并展示真实已安装的安卓 APP。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".EmulatorApplication"
        android:allowBackup="false"
        android:appCategory="game"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:preserveLegacyExternalStorage="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="r"
        tools:ignore="RtlEnabled">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:mimeType="application/java" />
                <data android:mimeType="application/java-archive" />
                <data android:mimeType="application/x-java-archive" />
                <data android:mimeType="text/vnd.sun.j2me.app-descriptor" />
                <data android:host="*" />
                <data android:scheme="file" />
                <data android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:mimeType="text/vnd.sun.j2me.app-descriptor" />
                <data android:host="*" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:mimeType="*/*" />
                <data android:host="*" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:pathPattern=".*.jar" />
                <data android:pathPattern=".*..*.jar" />
                <data android:pathPattern=".*..*..*.jar" />
                <data android:pathPattern=".*..*..*..*.jar" />
                <data android:pathPattern=".*.JAR" />
                <data android:pathPattern=".*..*.JAR" />
                <data android:pathPattern=".*..*..*.JAR" />
                <data android:pathPattern=".*..*..*..*.JAR" />
                <data android:pathPattern=".*.jad" />
                <data android:pathPattern=".*..*.jad" />
                <data android:pathPattern=".*..*..*.jad" />
                <data android:pathPattern=".*..*..*..*.jad" />
                <data android:pathPattern=".*.JAD" />
                <data android:pathPattern=".*..*.JAD" />
                <data android:pathPattern=".*..*..*.JAD" />
                <data android:pathPattern=".*..*..*..*.JAD" />
                <data android:pathPattern=".*.kjx" />
                <data android:pathPattern=".*..*.kjx" />
                <data android:pathPattern=".*..*..*.kjx" />
                <data android:pathPattern=".*..*..*..*.kjx" />
                <data android:pathPattern=".*.KJX" />
                <data android:pathPattern=".*..*.KJX" />
                <data android:pathPattern=".*..*..*.KJX" />
                <data android:pathPattern=".*..*..*..*.KJX" />
            </intent-filter>
        </activity>
        <activity
            android:name=".nokia.NokiaDesktopActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:stateNotNeeded="true"
            android:screenOrientation="portrait">
            <!-- 标准 Launcher 过滤器（包含 DEFAULT） -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- 抽屉应用列表展示入口 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".config.ConfigActivity"
            android:exported="true"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="javax.microedition.shell.MicroActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:process=":midlet" />
        <activity android:name=".settings.SettingsActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".settings.KeyMapperActivity" android:exported="false" />
        <activity
            android:name=".filepicker.FilteredFilePickerActivity"
            android:exported="false"
            android:theme="@style/FilePickerTheme" />
        <activity android:name=".config.ProfilesActivity" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/nnf_provider_paths" />
        </provider>
        <provider
            android:name=".storage.DocumentProvider"
            android:authorities="${applicationId}.documentProvider"
            android:enabled="@bool/isAtLeastQ"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
        <receiver
            android:name="com.nokia.mid.ui.NotificationBroadcastReceiver"
            android:process=":midlet" />

        <!-- jar 挂机保活前台服务（:midlet 进程，仅挂机期间常驻通知，点击回 jar 续跑）。
             API 34+ 要求前台服务声明类型，specialUse + 子类型说明。 -->
        <service
            android:name=".nokia.NokiaMidletKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:process=":midlet">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep J2ME midlet process alive while user backgrounds it (hangup feature)" />
        </service>

        <!-- 挂机 jar 清除通道（后台管理组件显式广播 → :midlet 进程内优雅销毁） -->
        <receiver
            android:name=".nokia.NokiaMidletControlReceiver"
            android:exported="false"
            android:process=":midlet" />

        <!-- 设备管理员：仅用于桌面「锁屏」一键锁屏息屏 -->
        <receiver
            android:name=".nokia.NokiaDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- 锁屏广播接收器：供 native 拦截器通过 am broadcast 触发 Device Admin 锁屏 -->
        <receiver
            android:name=".nokia.NokiaLockReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ru.playsoftware.j2meloader.nokia.LOCK_SCREEN" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
